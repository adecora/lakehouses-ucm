[
    {
        "source": {
            "format": "json",
            "options": {
            },
            "schema": "event_id string, event_ts datetime, session_id string, customer_id string, event_type string, product_id string, channel string",
            "schemaEvolutionMode": "rescue",
            "path": "app_events_??.json"
        },
        "sink": {
            "name": "events_raw",
            "execution": "{'availableNow': True}"
        }
    },
    {
        "source": {
            "format": "json",
            "options": {
            },
            "schema_location": "orders_cdc_??.json",
            "schemaEvolutionMode": "addNewColumns",
            "path": "orders_cdc_%%.json"
        },
        "sink": {
            "name": "orders_raw",
            "execution": "{'once': True}"
        }
    }
]