# This is a Declarative Automation Bundle definition for lakehouses_ucm.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: lakehouses_ucm
  uuid: 30fefbd0-c73e-448e-92f3-a25b331201f8

include:
  - "./resources/variables.yml"
  - "./resources/pipeline/lakehouses_ucm.pipeline.yml"
  - "./resources/job/lakehouses_ucm.job.yml"

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405614992691538.18.azuredatabricks.net
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
